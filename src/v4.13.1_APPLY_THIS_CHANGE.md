# v4.13.1 - Manuelle Code-Änderung erforderlich

## ⚠️ WICHTIG: Manuelle Änderung notwendig

Aufgrund von Escape-Sequenzen im Alert kann Figma Make diese Änderung nicht automatisch durchführen.

---

## 📝 Schritt 1: Öffne die Datei

`/screens/admin/LearningManagementScreen.tsx`

---

## 📝 Schritt 2: Finde Zeile 510-524

Suche nach diesem Code-Block (ca. Zeile 510):

```tsx
                  <CardContent>
                    <div className="text-center text-gray-400 py-12">
                      <ClipboardList className="w-16 h-16 mx-auto mb-4 opacity-50" />
                      <p className="text-lg font-medium mb-2">Noch keine Tests erstellt</p>
                      <p className="text-sm mb-4">Erstelle deinen ersten Test mit dem Test-Builder</p>
                      <Button
                        onClick={() => {
                          alert('Test Builder kommt in Phase 2! 🎓\n\n10 Block-Typen:\n- Multiple Choice\n- True/False\n- Text Input\n- Number Input\n- Date\n- Single Choice\n- Checkboxes\n- Dropdown\n- Slider\n- File Upload');
                        }}
                      >
                        <Plus className="w-4 h-4 mr-2" />
                        Test erstellen
                      </Button>
                    </div>
                  </CardContent>
```

---

## 📝 Schritt 3: Ersetze mit diesem Code

```tsx
                  <CardContent>
                    <BrowoKo_TestsList />
                  </CardContent>
```

**DAS WARS!** Einfach den gesamten Div-Block durch eine Zeile ersetzen.

---

## ✅ Ergebnis

Nach der Änderung sieht der komplette TabsContent so aus:

```tsx
              <TabsContent value="created" className="mt-6">
                <Card className="mobile-card md:desktop-card">
                  <CardHeader>
                    <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
                      <CardTitle className="flex items-center gap-2">
                        <ClipboardList className="w-5 h-5 text-blue-600" />
                        Meine Tests
                      </CardTitle>
                      
                      {/* Test Search */}
                      <div className="relative w-full md:w-64">
                        <Search className="absolute left-3 top-1/2 transform -translate-y-1/2 w-4 h-4 text-gray-400" />
                        <Input
                          type="text"
                          placeholder="Tests durchsuchen..."
                          value={testSearch}
                          onChange={(e) => setTestSearch(e.target.value)}
                          className="pl-10 w-full"
                        />
                      </div>
                    </div>
                  </CardHeader>
                  <CardContent>
                    <BrowoKo_TestsList />
                  </CardContent>
                </Card>
              </TabsContent>
```

---

## 🎯 Warum diese Änderung?

- Die **Header & Search** können wir später optional entfernen
- Die `BrowoKo_TestsList` Component hat bereits:
  - ✅ Eigenen Header
  - ✅ Eigene Search-Funktion
  - ✅ Empty State
  - ✅ Grid Layout

**ABER:** Für jetzt lassen wir die Card/CardHeader, weil das Design konsistent bleibt!

---

## 🚀 Nach der Änderung

1. Speichere die Datei
2. Die App lädt neu
3. Gehe zu: **Admin → Lernverwaltung → Tests → Erstellte Tests**
4. Klicke auf **"Test erstellen"**
5. Erstelle deinen ersten Test! 🎉

---

## 📦 Was dann funktioniert

✅ Tests erstellen mit vollständigem Dialog  
✅ Tests bearbeiten (Edit-Button → Phase 2)  
✅ Tests löschen mit Confirmation  
✅ Tests duplizieren  
✅ Tests suchen mit Live-Filter  
✅ Empty State wenn keine Tests  

---

## 🔥 Nächster Schritt: SQL für Supabase

Siehe: `/v4.13.1_SUPABASE_MIGRATION.sql`
