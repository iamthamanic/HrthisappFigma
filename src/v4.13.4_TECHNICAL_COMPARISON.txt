═══════════════════════════════════════════════════════════════
  v4.13.4: TECHNICAL COMPARISON - VORHER/NACHHER
═══════════════════════════════════════════════════════════════


┌─────────────────────────────────────────────────────────────┐
│  FIX 1: UUID SYNTAX ERROR                                   │
└─────────────────────────────────────────────────────────────┘

❌ VORHER (BrowoKo_CreateTestDialog.tsx):
───────────────────────────────────────────────────────────────
const { data: assignments } = await supabase
  .from('test_video_assignments')
  .select('video_id, test_id, tests!inner(id)')
  .neq('tests.id', null); // ← STRING "null" statt NULL!
───────────────────────────────────────────────────────────────

PROBLEM:
  - .neq('tests.id', null) vergleicht mit STRING "null"
  - PostgreSQL erwartet UUID, bekommt String
  - ERROR: invalid input syntax for type uuid: "null"


✅ NACHHER (BrowoKo_CreateTestDialog.tsx):
───────────────────────────────────────────────────────────────
const { data: assignments } = await supabase
  .from('test_video_assignments')
  .select('video_id, test_id, tests!inner(id)');
  // INNER JOIN filtert automatisch!
───────────────────────────────────────────────────────────────

LÖSUNG:
  - INNER JOIN filtert automatisch orphaned assignments
  - Kein .neq() nötig
  - SQL ist sauber: WHERE test_id IN (SELECT id FROM tests)



┌─────────────────────────────────────────────────────────────┐
│  FIX 2: VIDEO BEREITS ZUGEWIESEN                            │
└─────────────────────────────────────────────────────────────┘

❌ VORHER (BrowoKo_learningService.ts):
───────────────────────────────────────────────────────────────
// Check if video already has a test assigned
const { data: existing } = await this.supabase
  .from('test_video_assignments')
  .select('*')
  .eq('video_id', videoId)
  .single();

if (existing) {
  throw new ApiError('Video hat bereits einen Test zugewiesen');
}

// Insert new assignment
await this.supabase
  .from('test_video_assignments')
  .insert({ test_id: testId, video_id: videoId });
───────────────────────────────────────────────────────────────

PROBLEM:
  1. Prüft ALLE assignments, auch orphaned ones
  2. Test wird gelöscht → assignment bleibt
  3. Neuer Test kann Video nicht verknüpfen
  4. UNIQUE constraint verhindert Insert


✅ NACHHER (BrowoKo_learningService.ts):
───────────────────────────────────────────────────────────────
// Check if video has assignment to EXISTING test
const { data: existing } = await this.supabase
  .from('test_video_assignments')
  .select('*, tests!inner(id)') // ← INNER JOIN!
  .eq('video_id', videoId)
  .single();

if (existing) {
  throw new ApiError('Video hat bereits einen Test zugewiesen');
}

// Auto-cleanup orphaned assignments
await this.supabase
  .from('test_video_assignments')
  .delete()
  .eq('video_id', videoId)
  .not('test_id', 'in', `(SELECT id FROM tests)`);

// Insert new assignment
await this.supabase
  .from('test_video_assignments')
  .insert({ test_id: testId, video_id: videoId });
───────────────────────────────────────────────────────────────

LÖSUNG:
  1. INNER JOIN prüft nur existierende Tests
  2. Orphaned assignments werden ignoriert
  3. Auto-Cleanup vor Insert
  4. UNIQUE constraint wird nicht verletzt



┌─────────────────────────────────────────────────────────────┐
│  SQL VERGLEICH                                              │
└─────────────────────────────────────────────────────────────┘

❌ VORHER:
───────────────────────────────────────────────────────────────
-- Check Video Assignment
SELECT * FROM test_video_assignments
WHERE video_id = 'abc-123';

-- Findet auch orphaned assignments!
-- Test gelöscht → assignment bleibt → ERROR
───────────────────────────────────────────────────────────────


✅ NACHHER:
───────────────────────────────────────────────────────────────
-- Check Video Assignment (nur existierende Tests)
SELECT tva.*, t.id
FROM test_video_assignments tva
INNER JOIN tests t ON tva.test_id = t.id
WHERE tva.video_id = 'abc-123';

-- Findet NUR assignments mit existierendem Test!
-- Orphaned assignments werden ignoriert
───────────────────────────────────────────────────────────────


-- Auto-Cleanup orphaned assignments
DELETE FROM test_video_assignments
WHERE video_id = 'abc-123'
  AND test_id NOT IN (SELECT id FROM tests);

-- Löscht nur orphaned assignments!
-- Existierende assignments bleiben intakt
───────────────────────────────────────────────────────────────



┌─────────────────────────────────────────────────────────────┐
│  DATENBANK-ZUSTAND                                          │
└─────────────────────────────────────────────────────────────┘

❌ VORHER:
───────────────────────────────────────────────────────────────
test_video_assignments:
┌──────────┬──────────┬──────────┐
│ video_id │ test_id  │ Status   │
├──────────┼──────────┼──────────┤
│ video-1  │ test-1   │ ✅ OK     │
│ video-2  │ test-999 │ ❌ Test   │ ← Orphaned!
│          │          │  gelöscht │
└──────────┴──────────┴──────────┘

PROBLEM:
  - Neuer Test kann video-2 NICHT verknüpfen
  - UNIQUE(video_id) constraint verhindert Insert
  - "Video bereits zugewiesen" Error
───────────────────────────────────────────────────────────────


✅ NACHHER:
───────────────────────────────────────────────────────────────
test_video_assignments:
┌──────────┬──────────┬──────────┐
│ video_id │ test_id  │ Status   │
├──────────┼──────────┼──────────┤
│ video-1  │ test-1   │ ✅ OK     │
│ video-2  │ test-2   │ ✅ OK     │ ← Neu!
└──────────┴──────────┴──────────┘

LÖSUNG:
  - Orphaned assignment automatisch gelöscht
  - Neuer Test erfolgreich verknüpft
  - Datenbank ist sauber
───────────────────────────────────────────────────────────────



┌─────────────────────────────────────────────────────────────┐
│  CODE-FLOW VERGLEICH                                        │
└─────────────────────────────────────────────────────────────┘

❌ VORHER:
───────────────────────────────────────────────────────────────
1. User wählt Video in CreateTestDialog
2. Dialog ruft linkTestToVideo(testId, videoId)
3. Service prüft: Video zugewiesen? → JA (orphaned!)
4. Service wirft Error: "Video bereits zugewiesen"
5. User sieht Fehler ❌
───────────────────────────────────────────────────────────────


✅ NACHHER:
───────────────────────────────────────────────────────────────
1. User wählt Video in CreateTestDialog
2. Dialog ruft linkTestToVideo(testId, videoId)
3. Service prüft: Video zugewiesen (existierender Test)? → NEIN
4. Service löscht orphaned assignments für video
5. Service erstellt neue assignment
6. User sieht Success: "Test erstellt! 🎯"
7. Video wird im Test Builder angezeigt ✅
───────────────────────────────────────────────────────────────



┌─────────────────────────────────────────────────────────────┐
│  PERFORMANCE IMPACT                                         │
└─────────────────────────────────────────────────────────────┘

Query Performance:
  ✅ INNER JOIN ist schneller als LEFT JOIN + Filter
  ✅ Auto-Cleanup ist eine einfache DELETE Query
  ✅ Keine zusätzlichen Round-Trips nötig

Database:
  ✅ Datenbank bleibt sauber (keine orphaned records)
  ✅ UNIQUE constraint funktioniert korrekt
  ✅ Keine manuellen Cleanups nötig



═══════════════════════════════════════════════════════════════
  ZUSAMMENFASSUNG
═══════════════════════════════════════════════════════════════

FIX 1: UUID Error
  - INNER JOIN ohne .neq()
  - Kein String-Vergleich mit NULL

FIX 2: Video Assignment
  - Check nur existierende Tests (INNER JOIN)
  - Auto-Cleanup orphaned assignments
  - UNIQUE constraint wird nicht verletzt

RESULT:
  ✅ Alle Fehler gefixt
  ✅ Datenbank bleibt sauber
  ✅ Performance ist gut
  ✅ Keine SQL-Migration nötig


═══════════════════════════════════════════════════════════════
  STATUS: 🎉 PRODUCTION READY
═══════════════════════════════════════════════════════════════
